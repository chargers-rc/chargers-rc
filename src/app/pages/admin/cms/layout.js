export const cmsLayout = {
  // Spacing scale (Tracks & Classes)
  spacing: {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
    xxl: 32,
  },

  // Standard control height (inputs, selects, buttons)
  controlHeight: 38,

  // Standard row layout for forms
  row: {
    display: "flex",
    flexWrap: "wrap",
    alignItems: "flex-end",
    gap: "12px",
  },

  // Standard column flex for responsive inputs
  column: {
    flex: "1 1 200px",
  },

  // Card padding (Tracks & Classes)
  cardPadding: {
    padding: "12px 16px",
  },

  // Section header spacing
  sectionHeader: {
    marginBottom: "24px",
  },

  // Labels
  label: {
    fontSize: "13px",
    fontWeight: 500,
    marginBottom: "4px",
  },

  // Muted text
  muted: {
    fontSize: "12px",
    color: "#6B7280",
  },
};
